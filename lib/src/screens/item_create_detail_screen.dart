import 'package:flutter/material.dart';

class ItemCreateDetailScreen extends StatelessWidget {
  const ItemCreateDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
